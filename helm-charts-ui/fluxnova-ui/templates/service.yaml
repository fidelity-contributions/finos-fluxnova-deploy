apiVersion: v1
kind: Service
metadata:
  name: {{ include "fluxnova-ui.fullname" . }}
  labels:
    {{- include "fluxnova-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.containerPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "fluxnova-ui.selectorLabels" . | nindent 4 }}

